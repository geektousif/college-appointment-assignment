import * as appointmentValidator from './appointment.validator';
import * as slotValidator from './slot.validator';
import * as userValidator from './user.validator';

export { appointmentValidator, slotValidator, userValidator };
