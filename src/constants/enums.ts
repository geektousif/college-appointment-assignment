export enum USER_ROLE {
    STUDENT = 'student',
    PROFESSOR = 'professor',
}

export enum APPOINTMENT_STATUS {
    SCHEUDLED = 'scheduled',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
}
